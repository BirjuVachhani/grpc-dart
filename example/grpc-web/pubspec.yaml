name: grpc_web
description: Dart gRPC-Web sample client
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  grpc:
    path: ../../
  protobuf: ^3.1.0
  web: ">=1.1.0 <2.0.0"

dev_dependencies:
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.1
  lints: ^5.1.1
