name: route_guide
description: Dart gRPC sample client and server.
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  async: ">=2.7.0 <3.0.0"
  grpc:
    path: ../../
  protobuf: ^3.1.0
  collection: ">=1.19.0 <2.0.0"

dev_dependencies:
  lints: ^5.1.1
