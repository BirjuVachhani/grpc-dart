name: googleapis
description: Dart gRPC client sample for Google APIs
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  async: ">=2.7.0 <3.0.0"
  fixnum:
  grpc:
    path: ../../
  protobuf: ^3.1.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
